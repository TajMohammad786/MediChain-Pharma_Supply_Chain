const Dashboard = () => {
  // const [webData] = useOutletContext()
  // const { account, supplyChain, web3 } = webData
  // console.log(account, supplyChain, web3)
  return <div>Admin dashboard</div>;
};

export default Dashboard;
